import { Request, Response, NextFunction } from 'express';
// import { MonitoringAbsensiService } from './../services/monitoring-absensi.service';

export class MonitoringAbsensiController {
  // private service = new MonitoringAbsensiService();

  // controller here
}